{
  "name": "ssh-remote-monitoring-tool",
  "private": true,
  "scripts": {
    "web:build:firebase": "npm run build:firebase --prefix web",
    "firebase:emulators": "bash scripts/firebase-emulators.sh",
    "firebase:deploy": "bash scripts/firebase-deploy.sh",
    "firebase:deploy:hosting": "npm run web:build:firebase && firebase deploy --only hosting",
    "firebase:deploy:functions": "firebase deploy --only functions",
    "firebase:deploy:firestore": "firebase deploy --only firestore"
  },
  "devDependencies": {
    "firebase-tools": "^13.29.0"
  }
}
