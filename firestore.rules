rules_version = '2';

// API는 Cloud Functions(Admin SDK)만 Firestore에 접근합니다.
// 클라이언트는 HTTPS Functions 경유로만 데이터를 읽습니다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
